# Poker Game Frontend Environment Variables

# Optional: Base URL for the backend API (defaults to relative /api if not set)
# VITE_API_BASE_URL=http://localhost:8080

# Optional: WebSocket URL for STOMP (defaults to relative /ws if not set)
# VITE_WS_URL=http://localhost:8080/ws
